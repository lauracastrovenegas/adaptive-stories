import React from 'react';
import styled from 'styled-components';

const TimelineWrapper = styled.div`
  border: 1px solid black;
  padding 2rem;
`;

const Timeline = () => {
  return (
    <TimelineWrapper>This is the timeline feature</TimelineWrapper>
  );
};

export default Timeline;
